import 'package:app_pelu/src/util/bloc/contacto_bloc.dart';

class UserProvider{
  getIdVoucher(ContactoBloc contactoBloc){
    return Future.delayed(Duration(seconds: 5));
  }
  getUser(String id){
    return Future.delayed(Duration(seconds: 5));
  }
  setUser(ContactoBloc contactoBloc){
    return Future.delayed(Duration(seconds: 5));
  }
  getTurnos(){
    return Future.delayed(Duration(seconds: 5));
  }
}